apiVersion: v1
kind: Service
metadata:
  name: acme-cert
spec:
  ports:
  - nodePort: 30082
    port: 8080
    protocol: TCP
    targetPort: 8080
  type: NodePort
  selector:
    job-name: acme-cert
status:
  loadBalancer: {}